import { createClient } from '@supabase/supabase-js';

// Hardcode dummy keys to see if the syntax parser at least runs, we won't be able to connect if keys are invalid, but the error will be different from ERR_MODULE_NOT_FOUND. Or we can just build.
